extends layout

block content
    #logo
        img(src = "/images/logo.svg" alt = "main logo")
        p 
           | Alex Norton<br>
           | UX Design / Systems

    // Wrapper
    #wrapper

        // IMAGE TEST
        for result in projects
            h2= result.name
            .hero(style='background-image: url(result.images[0].src);')
                img.hero-img(src= result.images[0].src alt = result.images[0].caption)

        // /IMAGE TEST

    // /WRAPPER